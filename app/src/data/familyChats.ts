// Backward-compatible re-export. The domain model now lives in src/store/model.ts
// (types, seed data, ledger derivations); mutable state lives in src/store/AppStore.tsx.
export * from '../store/model';
